<!-- FILE: editarTE.ejs -->
<!DOCTYPE html>
<html lang="pt-br" data-bs-theme="dark">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Relação Treino-Equipamento</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/"><i class="bi bi-barbell"></i> GymManager</a>
        </div>
    </nav>
    <main class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card border-secondary">
                    <div class="card-header">
                        <h1 class="h4 mb-0">Editar Relação Treino-Equipamento</h1>
                    </div>
                    <div class="card-body">
                        <form method="post">
                            <div class="mb-3">
                                <label for="Codigo" class="form-label">Código da Relação</label>
                                <input type="text" id="Codigo" name="Codigo" class="form-control" readonly
                                    value="<%= TE._id %>">
                            </div>
                            <div class="mb-3">
                                <label for="Trecodigo" class="form-label">Código do Treino</label>
                                <input type="text" id="Trecodigo" name="Trecodigo" class="form-control" required
                                    value="<%= TE.Trecodigo %>">
                            </div>
                            <div class="mb-3">
                                <label for="Equcodigo" class="form-label">Código do Equipamento</label>
                                <input type="text" id="Equcodigo" name="Equcodigo" class="form-control" required
                                    value="<%= TE.Equcodigo %>">
                            </div>
                            <div class="mb-3">
                                <label for="Series" class="form-label">Séries</label>
                                <input type="number" id="Series" name="Series" class="form-control" required
                                    value="<%= TE.Series %>">
                            </div>
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-warning fw-bold">Salvar Alterações</button>
                                <a href="/lstTE" class="btn btn-secondary">Voltar para a Lista</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>